<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

<!--    <item android:id="@+id/item1"-->
<!--        android:title="New"-->
<!--        android:icon="@drawable/freeicon"-->
<!--        android:orderInCategory="100"-->
<!--        app:showAsAction="always"/>-->

    <!--if showasAction always then icon always

    if show actions is never the icon will never use
    the tittle shows
    -->
    <item android:id="@+id/newItem"
        android:title="New"
        android:icon="@drawable/freeicon"
        android:orderInCategory="500"
        app:showAsAction="never"/>
    <!--pay attention on the showAsAction this will determine the position of the item
    never will be overfloat

    note on never and
    //using the appcompat version for the actionViewClass
    -->

    <item android:id="@+id/search_item"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        android:icon="@android:drawable/ic_search_category_default"
        app:showAsAction="always|collapseActionView"
        android:title="Hello"/>


</menu>
